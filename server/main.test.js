/* eslint-disable */
describe('Test de suma, correcta', function () {
  it('El valor que se obtiene de la suma es el correcto', function () {
    // This code will be executed by the test driver when the app is started
    // in the correct mode
  })
})

describe('Test de suma, crasheo', function () {
  it('La api no crashea al ingresar otro tipo de valor que no sea numero', function () {
    // This code will be executed by the test driver when the app is started
    // in the correct mode
  })
})

describe('Ruteo de la api funciona correctamente', function () {
  it('Al ingresar una ruta deseada en la barra de navegacion va a dicha pagina', function () {
    // This code will be executed by the test driver when the app is started
    // in the correct mode
  })
})

describe('Ruteo de la api a rutas indefinidas', function () {
  it('Si se especifica una ruta que no esta definida, se debe devolver un mensaje de ruta no encontrada', function () {
    // This code will be executed by the test driver when the app is started
    // in the correct mode
  })
})
